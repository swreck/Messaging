{
  "name": "maria-messaging",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:local": "BASE_URL=http://localhost:5173 playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
